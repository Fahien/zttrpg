{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "$id": "skill-kind.schema.json",
    "title": "Skill kind",
    "description": "A category of skills, indicating if it has a base chance.",
    "type": "object",
    "required": ["name", "base_chance"],
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the skill kind."
        },
        "base_chance": {
            "type": "boolean",
            "description": "Indicates if this skill kind has a base chance."
        }
    }
}
